{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/my-bot-2.0/ui/settings.schema.json",
  "title": "My Bot UI settings metadata",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "surface", "title", "description", "sections"],
  "properties": {
    "schema_version": {"const": 1},
    "surface": {"type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
    "title": {"type": "string", "minLength": 3, "maxLength": 80},
    "description": {"type": "string", "minLength": 20, "maxLength": 500},
    "sections": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/section"}
    }
  },
  "$defs": {
    "option": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "value",
        "label",
        "summary",
        "description",
        "availability",
        "capability_ids",
        "prerequisites",
        "disabled_reason",
        "recommended",
        "warning"
      ],
      "properties": {
        "value": {"type": "string", "minLength": 1, "maxLength": 80},
        "label": {"type": "string", "minLength": 2, "maxLength": 100},
        "summary": {"type": "string", "minLength": 10, "maxLength": 180},
        "description": {"type": "string", "minLength": 30, "maxLength": 800},
        "availability": {"enum": ["available", "gated", "planned", "unsupported"]},
        "capability_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 3, "maxLength": 128}
        },
        "prerequisites": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 3, "maxLength": 200}
        },
        "disabled_reason": {"type": "string", "maxLength": 500},
        "recommended": {"type": "boolean"},
        "warning": {"type": "string", "maxLength": 500}
      }
    },
    "validation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "minimum": {"type": "integer"},
        "maximum": {"type": "integer"},
        "step": {"type": "integer", "minimum": 1},
        "max_length": {"type": "integer", "minimum": 1},
        "max_items": {"type": "integer", "minimum": 1}
      }
    },
    "setting": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "type",
        "label",
        "summary",
        "description",
        "default",
        "required",
        "engine_binding"
      ],
      "properties": {
        "id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$"},
        "type": {"enum": ["select", "instance-select", "integer", "boolean", "profile-queue"]},
        "label": {"type": "string", "minLength": 2, "maxLength": 100},
        "summary": {"type": "string", "minLength": 10, "maxLength": 180},
        "description": {"type": "string", "minLength": 30, "maxLength": 1000},
        "default": {},
        "required": {"type": "boolean"},
        "engine_binding": {"type": "string", "pattern": "^[A-Za-z][A-Za-z0-9]*(?:\.[A-Za-z_][A-Za-z0-9_]*)+$"},
        "options": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "#/$defs/option"}
        },
        "empty_state": {"type": "string", "minLength": 10, "maxLength": 300},
        "unit": {"type": "string", "minLength": 1, "maxLength": 40},
        "validation": {"$ref": "#/$defs/validation"}
      },
      "allOf": [
        {
          "if": {"properties": {"type": {"const": "select"}}},
          "then": {"required": ["options"]}
        },
        {
          "if": {"properties": {"type": {"const": "integer"}}},
          "then": {"required": ["validation"]}
        }
      ]
    },
    "section": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "title", "description", "order", "settings"],
      "properties": {
        "id": {"type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
        "title": {"type": "string", "minLength": 2, "maxLength": 100},
        "description": {"type": "string", "minLength": 20, "maxLength": 500},
        "order": {"type": "integer", "minimum": 0},
        "settings": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "#/$defs/setting"}
        }
      }
    }
  }
}
